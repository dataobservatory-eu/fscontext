% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_scan.R
\name{save_scan}
\alias{save_scan}
\title{Persist an observational snapshot to disk}
\usage{
save_scan(df, storage_id, path, label = NULL)
}
\arguments{
\item{df}{Data.frame. Scan result with created with \code{\link[=scan_storage]{scan_storage()}} that
has a \code{created_at} attribute.}

\item{storage_id}{Character. Identifier of the storage.}

\item{path}{Character. Directory where the file will be saved.}

\item{label}{Character. Optional human-readable label describing
the scanned scope (e.g. "d_eviota"). Default is \code{NULL} (no label).}
}
\value{
Invisibly returns the full file path of the saved \code{.rds} file.
}
\description{
Saves a snapshot dataset (typically produced by \code{\link[=scan_storage]{scan_storage()}})
as a uniquely identified \code{.rds} file.
}
\details{
The filename encodes:
\itemize{
\item storage context
\item observation time
\item optional scope label
\item a deterministic hash
}

This ensures:
\itemize{
\item chronological ordering
\item uniqueness
\item reproducibility of stored observations
}
}
\examples{
tmp_dir <- tempfile()
dir.create(tmp_dir)

dir.create(file.path(tmp_dir, "R"))
dir.create(file.path(tmp_dir, "data"))

file.create(file.path(tmp_dir, "R", "a.R"))
file.create(file.path(tmp_dir, "R", "b.R"))
file.create(file.path(tmp_dir, "data", "c.csv"))

scan_storage(
  root = tmp_dir,
  storage_id = "test-storage"
)

save_scan(scan, "test-storage")

}
