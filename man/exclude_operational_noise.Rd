% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exclude_operational_noise.R
\name{exclude_operational_noise}
\alias{exclude_operational_noise}
\title{Exclude operational noise from analytical workflows}
\usage{
exclude_operational_noise(
  x,
  filename = "filename",
  extension = "extension",
  profiles = c("generic", "rstudio")
)
}
\arguments{
\item{x}{A \code{data.frame} or tibble containing contextual or
analytical reconstruction entities.}

\item{filename}{Character scalar identifying the filename column.

Defaults to \code{"filename"}.}

\item{extension}{Character scalar identifying the extension column.

Defaults to \code{"extension"}.}

\item{profiles}{Character vector defining operational noise
profiles to apply.

Current profiles include:
\itemize{
\item \code{"generic"}
\item \code{"rstudio"}
}}
}
\value{
A filtered \code{data.frame} excluding operational noise resources.
}
\description{
Excludes operationally low-value system and workflow artifacts from
analytical reconstruction workflows while preserving the original
observational evidence.

The function is designed for provenance-aware analytical pipelines
where certain operational artifacts may:
\itemize{
\item inflate duplication metrics;
\item distort reuse analysis;
\item obscure meaningful reconstruction patterns;
\item or reduce review efficiency.
}

Unlike destructive filtering, the function is intended to operate on
contextual or analytical reconstruction layers after observational
evidence has already been preserved.

This distinction is important for:
\itemize{
\item forensic reproducibility;
\item provenance-aware reconstruction;
\item archival transparency;
\item and Heritage Digital Twin workflows.
}

The function supports lightweight operational noise profiles.

Current built-in profiles include:
\itemize{
\item \code{"generic"}
\item \code{"rstudio"}
}

The \code{"generic"} profile targets common system and synchronization
artifacts.

The \code{"rstudio"} profile targets operational artifacts commonly
produced during R and RStudio workflows.

Future versions may support:
\itemize{
\item workflow-specific profiles;
\item institution-specific registries;
\item YAML-based noise vocabularies;
\item preservation-oriented filtering policies.
}

The function is designed to work together with:
\itemize{
\item \code{\link[=read_snapshot]{read_snapshot()}}
\item \code{\link[=snapshot_to_reconstruction_context]{snapshot_to_reconstruction_context()}}
}

as part of layered provenance-aware reconstruction workflows.
}
\details{
The function intentionally excludes only operationally low-priority
resources.

It does not:
\itemize{
\item delete observational evidence;
\item modify the original snapshot data;
\item infer preservation value;
\item determine archival significance;
\item replace curatorial review.
}

Resources excluded from analytical workflows may still remain
important for:
\itemize{
\item forensic preservation;
\item synchronization reconstruction;
\item reproducibility auditing;
\item or operational environment analysis.
}
}
\examples{
toy_files <- tibble::tibble(
  filename = c(
    ".DS_Store",
    ".Rhistory",
    "analysis.R",
    "report.qmd"
  ),
  extension = c(
    "",
    "",
    "R",
    "qmd"
  )
)

exclude_operational_noise(
  toy_files,
  profiles = c(
    "generic",
    "rstudio"
  )
)

}
