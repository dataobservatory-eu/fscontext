% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_snapshot_context.R
\name{add_snapshot_context}
\alias{add_snapshot_context}
\title{Add contextual identifiers to snapshot observations}
\usage{
add_snapshot_context(df)
}
\arguments{
\item{df}{A snapshot \code{data.frame} created by \code{\link[=scan_storage]{scan_storage()}}
or \code{\link[=read_snapshot]{read_snapshot()}}.}
}
\value{
A \code{data.frame} enriched with additional contextual identifier
variables.
}
\description{
Enriches filesystem observations with deterministic contextual
identifiers used for longitudinal, cross-storage, and forensic
reconstruction workflows.
}
\details{
The function preserves the original observational rows and adds
contextual identifiers derived from:
\itemize{
\item storage context;
\item filesystem location;
\item observation time.
}

The package deliberately separates filesystem observation from
later analytical interpretation and documentary aggregation.

The function therefore:
\itemize{
\item enriches filesystem observations with contextual identifiers;
\item supports repeated observation tracking across time;
\item supports comparison across storage systems;
\item does not construct Record Sets or higher-level documentary
aggregations.
}

The added identifiers support:
\itemize{
\item reconstruction of distributed work environments;
\item provenance-aware analytical workflows;
\item longitudinal filesystem analysis;
\item cross-storage comparison of observations.
}

Added variables:
\itemize{
\item \code{storage_full_path}:
globally contextualised filesystem locator
(\code{storage_id::full_path});
\item \code{storage_path_id}:
deterministic storage-scoped filesystem identifier
(\code{storage_id::rel_path});
\item \code{observation_id}:
deterministic identifier of a specific filesystem observation,
combining storage context, relative path, and observation time.
}
}
\examples{
data("fscontextdemo_snapshot_02")

snapshots <- add_snapshot_context(fscontextdemo_snapshot_02)

head(
  snapshots[
    ,
    c(
      "storage_full_path",
      "storage_path_id",
      "observation_id"
    )
  ]
)
}
