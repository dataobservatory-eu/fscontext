% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/semantic_stabilisation.R
\name{semantic_stabilisation}
\alias{semantic_stabilisation}
\alias{semantic_stabilization}
\title{Evaluate semantic stabilization readiness}
\usage{
semantic_stabilisation(x)

semantic_stabilization(x)
}
\arguments{
\item{x}{A semantically enriched vector, typically a
\code{prelabelled} vector.}
}
\value{
A named list containing:

\describe{
\item{valid}{Logical. \code{TRUE} if coercion succeeded.}
\item{message}{Diagnostic warning or error message, if any.}
}
}
\description{
Evaluate whether a semantically enriched observational vector
can be coerced into stricter semantic representations.
}
\details{
The function currently evaluates whether a vector with
provisional semantic overlays can be represented as a
\code{\link[labelled:labelled]{labelled::labelled()}} vector.

Semantic stabilization is interpreted progressively:
\itemize{
\item observational vectors preserve raw observed values;
\item \code{prelabelled} vectors attach provisional semantic overlays;
\item stricter semantic representations may require additional
semantic normalization or stabilization.
}

The function therefore does not evaluate semantic correctness,
but rather structural readiness for coercion into stricter
semantic representations.
}
\examples{
x <- prelabel(
  c("r", "png", "csv"),
  labels = c(
    r = "source_code",
    png = "visualisation",
    csv = "tabular_data"
  )
)

semantic_stabilisation(x)

}
