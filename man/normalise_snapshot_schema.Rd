% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalise_snapshot_schema.R
\name{normalise_snapshot_schema}
\alias{normalise_snapshot_schema}
\title{Normalise snapshot schema to the current version}
\usage{
normalise_snapshot_schema(df)
}
\arguments{
\item{df}{A \code{data.frame} representing a filesystem snapshot.}
}
\value{
A \code{data.frame} conforming to the current snapshot schema.
}
\description{
Ensures that a snapshot \code{data.frame} conforms to the canonical
schema used by the package.
}
\details{
This function detects the schema version of the input (via the
\code{schema_version} attribute) and applies the necessary migration
steps to bring it to the current version.

Currently supported:
\itemize{
\item snapshots without a \code{schema_version} attribute are assumed to
originate from version 0.1.0 and are migrated accordingly
\item snapshots with \code{schema_version = "0.1.0"} are migrated to
version 0.1.2
}

The function is safe to call on already-normalised data and may
be used at the start of analytical workflows to ensure consistency.

The canonical schema includes:
\itemize{
\item \code{rel_path} as the primary file-instance identifier
\item \code{filename} as the basename of the file
\item \code{dir_path} derived from \code{rel_path}
}

Additional columns from \code{scan_storage()} are preserved.
}
\examples{
data("fscontextdemo_snapshot_02")
snap <- normalise_snapshot_schema(fscontextdemo_snapshot_02)

}
\keyword{internal}
