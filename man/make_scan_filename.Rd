% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_scan_filename.R
\name{make_scan_filename}
\alias{make_scan_filename}
\title{Create a stable filename for an observational snapshot}
\usage{
make_scan_filename(storage_id, scan_time = Sys.time(), label = NULL)
}
\arguments{
\item{storage_id}{Character. Identifier of the storage context.}

\item{scan_time}{POSIXct. Timestamp of the observation event
(default: current time).}

\item{label}{Character. Optional label describing the observed scope.}
}
\value{
Character. Snapshot filename ending in \code{.rds}.
}
\description{
Generates a filesystem-safe, chronologically sortable filename
for storing observational snapshot artefacts.
}
\details{
The filename encodes:
\itemize{
\item storage context (\code{storage_id})
\item optional observational scope (\code{label})
\item scan timestamp
\item a short deterministic uniqueness hash
}

The generated filename acts as a lightweight external identifier
for a stored observational snapshot.

The naming scheme is intended to support:
\itemize{
\item reproducible archival storage
\item chronological ordering
\item provenance-aware reconstruction
\item comparison of repeated filesystem observations
}

The generated hash is deterministic for identical inputs and helps
avoid filename collisions between similar scan events.

The function does not assign documentary identity or Record Set
semantics. It creates identifiers for stored observational artefacts.
}
\keyword{internal}
