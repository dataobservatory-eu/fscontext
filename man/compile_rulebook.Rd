% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compile_rulebook.R
\name{compile_rulebook}
\alias{compile_rulebook}
\title{Compile a semantic refinement rulebook}
\usage{
compile_rulebook(rulebook)
}
\arguments{
\item{rulebook}{A data frame or tibble containing semantic
refinement rules.

The input must contain:
\itemize{
\item \code{"refine_id"}: refinement stage identifier;
\item \code{"variable"}: matching variable;
\item \code{"match"}: matching semantics;
\item \code{"pattern"}: matching pattern;
\item \code{"refined_assertion"}: refined semantic assertion.
}}
}
\value{
A list of compiled semantic refinement specifications suitable
for \code{\link[=refine_by_rulebook]{refine_by_rulebook()}}.

Each list element contains:
\itemize{
\item \code{"refine_id"}: refinement stage identifier;
\item \code{"rules"}: wide matching rule table;
\item \code{"by"}: matching variables;
\item \code{"match"}: matching semantics;
\item \code{"assertion"}: refined semantic assertion.
}
}
\description{
Compile a tidy semantic refinement rulebook into grouped
refinement specifications suitable for
\code{\link[=refine_by_rulebook]{refine_by_rulebook()}} workflows.
}
\details{
\code{compile_rulebook()} transforms a long-form rule table into a
lightweight operational structure for iterative semantic
stabilization.

Each compiled refinement stage contains:
\itemize{
\item matching variables;
\item matching semantics;
\item observational matching patterns;
\item refined semantic assertions.
}

Rulebooks support workflows where semantic interpretations are
progressively stabilized through deterministic contextual
matching rather than fully predefined ontology structures.

The function intentionally preserves tidy relational semantics
in the rulebook representation while creating a lightweight
operational structure for iterative semantic refinement.

Rulebooks are designed to support workflows where semantic
stabilization emerges gradually through deterministic matching
operations rather than through fully predefined ontological
structures.
}
\examples{

rulebook <- data.frame(
  refine_id = c(
    "refine_1",
    "refine_1",
    "refine_2"
  ),
  variable = c(
    "extension",
    "filename",
    "extension"
  ),
  match = c(
    "exact",
    "starts_with",
    "exact"
  ),
  pattern = c(
    "png",
    "film",
    "csv"
  ),
  refined_assertion = c(
    "visualisation",
    "visualisation",
    "tabular_data"
  ),
  stringsAsFactors = FALSE
)

compile_rulebook(rulebook)

}
\seealso{
\code{\link[=refine]{refine()}},  \code{\link[=refine_by_rulebook]{refine_by_rulebook()}}
}
