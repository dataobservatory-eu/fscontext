% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snapshot_to_reconstruction_context.R
\name{snapshot_to_reconstruction_context}
\alias{snapshot_to_reconstruction_context}
\title{Reconstruct a contextual observational Record Set from filesystem snapshots}
\usage{
snapshot_to_reconstruction_context(
  snapshot_files,
  roots,
  exclude_patterns = "\\\\.Rcheck"
)
}
\arguments{
\item{snapshot_files}{Character vector of \code{.rds} snapshot files
created with \code{\link[=snapshot_storage]{snapshot_storage()}}.}

\item{roots}{Character vector of contextual root paths used
for observational selection.}

\item{exclude_patterns}{Character vector of exclusion patterns
passed to \code{\link[=subset_snapshot]{subset_snapshot()}}.}
}
\value{
A contextual observational reconstruction table enriched with:
\itemize{
\item contextual observational identifiers;
\item storage-aware path identifiers;
\item structural grouping heuristics;
\item lightweight contextual Record Set projections.
}

Core observational variables typically include:
\itemize{
\item \code{storage_id};
\item \code{person_id};
\item \code{full_path};
\item \code{rel_path};
\item \code{filename};
\item \code{extension};
\item \code{mtime};
\item \code{scan_time}.
}

Contextual enrichment variables may include:
\itemize{
\item \code{inst_id};
\item \code{storage_path_id};
\item \code{observation_id};
\item \code{structural_group};
\item \code{component};
\item \code{record_set_id};
\item \code{resource_id};
\item \code{locator_path}.
}
}
\description{
Reconstructs a contextual observational corpus from one or more
filesystem snapshot fragments.

The function:

\enumerate{
\item merges observational filesystem snapshots;
\item filters observations to selected contextual roots;
\item enriches observations with contextual identifiers;
\item derives lightweight structural grouping heuristics;
\item creates a contextual Record Set projection.
}

The workflow is optimized for:
\itemize{
\item forensic reconstruction;
\item filesystem archaeology;
\item exploratory analytical workflows;
\item development environment reconstruction;
\item operational reporting.
}

Unlike \code{\link[=snapshot_to_recordset_df]{snapshot_to_recordset_df()}}, this function intentionally
prioritizes analytical reconstruction over preservation-oriented
semantic assertions.
}
\details{
Snapshot fragments are merged observationally.

Duplicate filesystem observations are intentionally preserved
because the same resource may legitimately appear across:
\itemize{
\item multiple machines;
\item multiple storage contexts;
\item repeated scans;
\item synchronised working environments.
}

The resulting object remains observational and analytical.

Structural grouping heuristics are lightweight filesystem-derived
operational projections and do not imply authoritative archival
Record Set construction.

The function serves as the foundational reconstruction layer for:
\itemize{
\item analytical enrichment workflows;
\item reconstruction reporting;
\item semantic preservation wrappers such as
\code{\link[=snapshot_to_recordset_df]{snapshot_to_recordset_df()}}.
}
}
\examples{
data("fscontextdemo_snapshot_01")

tmp <- tempfile(fileext = ".rds")
saveRDS(fscontextdemo_snapshot_01, tmp)

snapshot_to_reconstruction_context(
  snapshot_files = tmp,
  roots = "D:/_packages/fscontextdemo/R"
)
}
\seealso{
\code{\link[=snapshot_to_recordset_df]{snapshot_to_recordset_df()}},
\code{\link[=subset_snapshot]{subset_snapshot()}},
\code{\link[=add_snapshot_context]{add_snapshot_context()}},
\code{\link[=add_structural_groups]{add_structural_groups()}},
\code{\link[=create_record_set]{create_record_set()}}
}
