% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refine.R
\name{refine}
\alias{refine}
\title{Refine semantic assertions through contextual matching}
\usage{
refine(x, target = NULL, rules, by, assertion, comment = NULL, match = "exact")
}
\arguments{
\item{x}{A data frame or tibble.}

\item{target}{Name of the target column to refine.}

\item{rules}{A rule table or compiled rulebook.}

\item{by}{Optional grouping variables used during refinement.}

\item{assertion}{Optional assertion text recorded in provenance.}

\item{comment}{Optional comment attached to the refinement step.}

\item{match}{Matching strategy. Defaults to \code{"first"}.}
}
\description{
\code{refine()} incrementally stabilizes semantic assertions through
deterministic contextual matching rules while preserving row
cardinality and the original observational universe.
}
\details{
The function is designed for lightweight semantic refinement
workflows where semantic interpretations mature gradually
through ordinary tidyverse operations.

Matching observations are identified through configurable
matching semantics applied to one or more observational
variables.

Supported matching semantics include:
\itemize{
\item \code{"exact"} relational equality;
\item \code{"starts_with"} hierarchical prefix matching;
\item \code{"ends_with"} suffix matching;
\item \code{"contains"} substring detection.
}

Matching positions in the target vector are replaced by
refined semantic assertions.

Unmatched values remain unchanged.

\code{refine()} intentionally never:
\itemize{
\item removes rows;
\item reshapes tables;
\item modifies unrelated observations.
}

This makes refinement stages auditable, reversible, and
compatible with iterative semantic stabilization workflows.

\code{refine()} operates on semantic operationalisations produced
through workflows such as:
\itemize{
\item \code{\link[=prelabel]{prelabel()}};
\item \code{\link[=as.character]{as.character()}};
\item \code{\link[=as_character]{as_character()}};
\item previous refinement stages.
}

Rather than enforcing formally complete ontology semantics,
the function provides a lightweight operational mechanism for
progressively stabilizing semantic interpretations inside
ordinary analytical workflows.

Multiple refinement stages may later mature into:
\itemize{
\item controlled vocabularies;
\item \code{\link[labelled:labelled]{labelled::labelled()}} vectors;
\item \code{\link[dataset:defined]{dataset::defined()}} vectors;
\item semantically enriched datasets
\item compatible with iterative semantic workflows.
}

\code{refine()} operates on semantic operationalisations produced
through workflows such as:
\itemize{
\item \code{\link[=prelabel]{prelabel()}};
\item \code{\link[=as.character]{as.character()}};
\item \code{\link[=as_character]{as_character()}};
\item earlier refinement stages.
}

The function does not attempt to construct formally complete
semantic graphs or enforce ontology-level consistency.

Instead, it provides a lightweight operational mechanism for
progressively stabilizing semantic interpretations inside
ordinary tidyverse workflows.

This approach is particularly useful when working with:
\itemize{
\item partially harmonised datasets;
\item inconsistent coding systems;
\item ambiguous metadata;
\item hierarchical filesystem structures;
\item exploratory semantic reconstruction workflows.
}

Multiple refinement stages may later mature into:
\itemize{
\item controlled vocabularies;
\item formally defined semantic vectors;
\item semantically enriched datasets;
\item or graph-based semantic representations.
}
}
\examples{

files <- tibble::tibble(
  filename = c(
    "filmA.png",
    "filmB.png",
    "film.xlsx",
    "fill.png"
  ),
  extension = c(
    "png",
    "png",
    "xlsx",
    "png"
  )
)

out <- refine(
  x = files,
  target =
    rep(
      "unresolved",
      nrow(files)
    ),
  rules =
    tibble::tibble(
      filename = "film",
      extension = "png"
    ),
  by = c(
    "filename",
    "extension"
  ),
  match = c(
    "starts_with",
    "exact"
  ),
  assertion =
    "film_visualisation"
)

out

}
\seealso{
\code{\link[=refine_by_rulebook]{refine_by_rulebook()}}, \code{\link[=compile_rulebook]{compile_rulebook()}}
}
