% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prelabelled.R
\name{prelabel}
\alias{prelabel}
\title{Create a prelabelled vector}
\usage{
prelabel(x, labels, unmatched = "keep", missing_label = "<NA>")
}
\arguments{
\item{x}{A vector.}

\item{labels}{Candidate semantic mappings describing
provisional semantic assertions.

\code{labels} is internally normalised with \code{\link[=as_value_key]{as_value_key()}} and
may therefore be supplied as:
\itemize{
\item a named vector;
\item a named list;
\item a two-column data frame or tibble.
}}

\item{unmatched}{Behaviour for unmatched observational values.

One of:
\itemize{
\item \code{"keep"} (default): preserve unmatched values as
observational semantic assertions;
\item \code{"na"}: operationalise unmatched values as \code{NA} during
semantic coercion.
}}

\item{missing_label}{Semantic assertion used internally for
missing observational values.}
}
\value{
A vector with:
\itemize{
\item class \code{"prelabelled"};
\item attached provisional semantic vocabulary stored in the
\code{"prelabel"} attribute.
}
}
\description{
Attach provisional semantic assertions to an observational
vector.
}
\details{
\code{prelabel()} creates lightweight semantic mappings that support
iterative semantic refinement workflows before values mature
into formally defined variables created with
\code{\link[labelled:labelled]{labelled::labelled()}} or \code{\link[dataset:defined]{dataset::defined()}}.

Unlike strictly defined semantic vectors, \code{prelabelled}
vectors tolerate:
\itemize{
\item incomplete semantic mappings;
\item unresolved observational values;
\item contextual ambiguity;
\item incremental semantic stabilisation.
}

This design is particularly useful in provenance-aware,
contextual reconstruction, and archival workflows where
semantic interpretations emerge gradually through iterative
refinement.

The class supports workflows inspired by RiC-O and PROV-O,
where observational evidence and semantic interpretation
remain explicitly separated.

\code{prelabelled} vectors intentionally separate:
\itemize{
\item observational evidence;
\item semantic operationalisation;
\item contextual semantic refinement.
}

Original observational values remain unchanged while semantic
assertions may evolve through iterative refinement workflows.

Semantic operationalisation is available through:
\itemize{
\item \code{\link[=as.character]{as.character()}} for lightweight semantic coercion;
\item \code{\link[=as_character]{as_character()}} for provenance-preserving semantic
workspaces.
}
}
\examples{

x <- c("R", "png", "csv", "unknown")

extension_map <- c(
  R = "functional_programming",
  png = "visualisation",
  csv = "tabular_data"
)

x <- prelabel(x, labels = extension_map)

x

as.character(x)

semantic_workspace <- as_character(x)

attributes(semantic_workspace)

}
\references{
Lebo, T., Sahoo, S., McGuinness, D. et al. (2013).
PROV-O: The PROV Ontology.
\url{https://www.w3.org/TR/prov-o/}

International Council on Archives Expert Group on Archival Description
(2023). Records in Contexts (RiC).
\url{https://www.ica.org/ica-network/expert-groups/egad/records-in-contexts-ric/}
}
\seealso{
\code{\link[dataset:prelabel]{dataset::prelabel()}},
\code{\link[dataset:defined]{dataset::defined()}},
\code{\link[dataset:as_value_key]{dataset::as_value_key()}}
}
