% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_storage.R
\name{scan_storage}
\alias{scan_storage}
\title{Observe a filesystem and construct a reproducible snapshot}
\usage{
scan_storage(
  root,
  storage_id = "l480-1-ssd",
  person_id = "antaldaniel",
  scan_time = Sys.time(),
  compute_signature = TRUE,
  max_signature_size = 200 * 1024 * 1024
)
}
\arguments{
\item{root}{Character. Path to the root folder to observe.}

\item{storage_id}{Character. Identifier of the storage context.}

\item{person_id}{Character. Identifier of the observer or operator.}

\item{scan_time}{POSIXct. Timestamp of the observation.
Defaults to \code{Sys.time()} if not provided.}

\item{compute_signature}{Logical. Whether to compute lightweight
content signatures.}

\item{max_signature_size}{Numeric. Maximum file size (bytes)
for signature computation.}
}
\value{
A \code{data.frame} where each row represents one filesystem observation.
}
\description{
Recursively scans a root folder and returns a data.frame where each row
represents one filesystem observation recorded at a specific time.
}
\details{
The function implements a read-only filesystem observation model:
\itemize{
\item it records accessible filesystem state;
\item it does not interpret file contents;
\item it does not assume canonical, complete, or authoritative state.
}

Each observation records:
\itemize{
\item a relative filesystem locator (\code{rel_path});
\item a storage context (\code{storage_id});
\item an observation timestamp (\code{scan_time}).
}

Additional metadata may include:
\itemize{
\item filesystem properties (size, timestamps, permissions);
\item optional content signatures (\code{quick_sig});
\item repository and version-control context
(\code{repo_root}, \code{repo_rel_path}, \code{git_tracked}).
}

The package deliberately records filesystem observations first and
postpones documentary interpretation, Record Set construction,
and RiC-aligned semantic assertions to later analytical stages.

This creates a reproducible observational snapshot suitable for:
\itemize{
\item forensic analysis of development environments;
\item reconstruction of activity patterns;
\item audit and compliance workflows;
\item alignment with version-controlled repositories.
}

The returned dataset at minimum contains:
\itemize{
\item \code{rel_path}: relative filesystem locator within the observed root;
\item \code{storage_path_id}: deterministic storage-scoped identifier derived
from \code{storage_id::rel_path};
\item \code{filename}: basename of the observed file;
\item \code{mtime}: last modification timestamp;
\item \code{extension}: file extension.
}

Additional variables may be present depending on scan configuration.

The function is:
\itemize{
\item read-only and non-destructive;
\item deterministic for a given filesystem state;
\item robust to inaccessible files, which are silently skipped.
}

The result represents observed filesystem state rather than complete
historical provenance.
}
\examples{
root <- system.file(
  "testdata/minimal_R_folder",
  package = "fscontext"
)

snapshot <- scan_storage(root)

subset(
  snapshot,
  rel_path \%in\% c(
    "DESCRIPTION",
    "NAMESPACE",
    "R/hello_world.R",
    "vignettes/demo.Rmd"
  )
)[, c("rel_path", "extension")]

}
\seealso{
\code{\link[=snapshot_storage]{snapshot_storage()}}
}
