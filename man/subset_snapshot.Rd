% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subset_snapshot.R
\name{subset_snapshot}
\alias{subset_snapshot}
\title{Subset observational filesystem Instantiations}
\usage{
subset_snapshot(
  snapshot_path,
  folder_path,
  extensions = NULL,
  exclude_patterns = c("\\\\.Rcheck")
)
}
\arguments{
\item{snapshot_path}{Character. Path to \code{.rds} snapshot file.}

\item{folder_path}{Character vector. One or more folder roots.}

\item{extensions}{Optional character vector of file extensions (no dot).}

\item{exclude_patterns}{Optional regex patterns to exclude paths.}
}
\value{
data.frame filtered snapshot with \code{rel_root_path}
}
\description{
Selects observations from a snapshot according to structural
criteria such as paths, extensions, or exclusion patterns.
}
\details{
The function performs observational selection only and does not
derive Record Sets, contextual hierarchies, or analytical groupings.

The function returns a subset of the original snapshot while preserving
its observational provenance (e.g. \code{created_by}, \code{created_at}).

In addition, it derives a \code{rel_root_path} column, which represents the
path of each file relative to the matched filter root. When multiple
\code{folder_path} values are provided, the deepest matching root is used.

The \code{rel_root_path} is a context-dependent projection intended for
grouping, navigation, and reporting. It is not a stable identifier and
should not be used for joins or identity; use \code{rel_path} for that purpose.
}
\examples{
data("fscontextdemo_snapshot_02")

tmp <- tempfile(fileext = ".rds")
saveRDS(fscontextdemo_snapshot_02, tmp)

subset_snapshot(
  snapshot_path = tmp,
  folder_path = "D:/_packages/fscontextdemo/R"
)
}
