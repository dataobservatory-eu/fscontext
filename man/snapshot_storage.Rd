% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snapshot_storage.R
\name{snapshot_storage}
\alias{snapshot_storage}
\title{Create and persist an observational snapshot of a filesystem}
\usage{
snapshot_storage(
  root,
  storage_id = "l480-ssd",
  person_id = "antaldaniel",
  scan_time = Sys.time(),
  label = NULL,
  path = here::here("data-raw", "snapshots"),
  compute_signature = TRUE,
  max_signature_size = 200 * 1024 * 1024
)
}
\arguments{
\item{root}{Character. Path to the root folder to scan.}

\item{storage_id}{Character. Identifier of the storage.}

\item{person_id}{Character. Identifier of the person.}

\item{scan_time}{POSIXct. Timestamp of the scan.}

\item{label}{Character. Optional human-readable label describing
the scanned scope (e.g. "d_eviota").}

\item{path}{Character. Directory where snapshots are stored.}

\item{compute_signature}{Logical. Whether to compute fast file signatures.}

\item{max_signature_size}{Numeric. Maximum file size in bytes for signatures.}
}
\value{
Invisibly returns the path to the stored snapshot.

Invisibly returns the full path to the saved snapshot.
}
\description{
Executes \code{\link[=scan_storage]{scan_storage()}} and stores the resulting observational dataset
as a timestamped \code{.rds} snapshot.
}
\details{
Each snapshot captures the \strong{state of file instantiations at a specific
point in time}, preserving:
\itemize{
\item file-level metadata
\item structural context
\item globally contextualised file paths (\code{storage_id::local_path})
\item optional content signatures
\item repository associations
}

Snapshots are intended as:
\itemize{
\item durable audit artefacts
\item inputs for longitudinal analysis
\item reproducible evidence of observed environments
}

Since schema version 0.1.3, snapshots store \code{full_path}
values as globally contextualised paths:

\code{storage_id::local_filesystem_path}

This prevents collisions between similar local folder structures
observed on different machines or storage contexts.
}
\examples{
\dontrun{
snapshot_storage("D:/_markdown", "l480-ssd", label = "d_markdown")
}

}
