% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refine_by_rulebook.R
\name{refine_by_rulebook}
\alias{refine_by_rulebook}
\title{Iteratively refine semantic assertions using a rulebook}
\usage{
refine_by_rulebook(
  x,
  target,
  rulebook,
  prefix = NULL,
  keep_intermediate = TRUE,
  final_name = "final"
)
}
\arguments{
\item{x}{A data frame.}

\item{target}{Bare or quoted name of the initial semantic
assertion column.}

\item{rulebook}{A compiled rulebook object created with
\code{compile_rulebook()}.}

\item{prefix}{Prefix used for generated refinement columns.
Defaults to the target column name.}

\item{keep_intermediate}{Logical.
If \code{TRUE}, keeps all intermediate refinement columns.
If \code{FALSE}, returns only the final refinement column.}

\item{final_name}{Name of the final refinement column.}
}
\value{
A tibble with progressively refined semantic assertions.
}
\description{
Apply a compiled semantic rulebook sequentially to a target
semantic assertion vector.
}
\details{
\code{refine_by_rulebook()} wraps an iterative refinement workflow
where each semantic refinement stage operates on the semantic
state produced by the previous stage.

The function is designed for operational semantic stabilization
workflows where semantic assertions progressively mature through
deterministic contextual refinement.

Each refinement stage creates a new refinement column,
preserving semantic progression for inspection, auditing, and
review.

Rulebooks are typically created with
\code{\link[=compile_rulebook]{compile_rulebook()}} and operate on semantic operationalisations
produced with:
\itemize{
\item \code{\link[=prelabel]{prelabel()}};
\item \code{\link[=as.character]{as.character()}};
\item \code{\link[=as_character]{as_character()}};
\item \code{\link[=refine]{refine()}}.
}

Conceptually, the function behaves similarly to an iterative
\code{purrr::reduce()} workflow applied to semantic refinement
stages.
}
\seealso{
\code{\link[=refine]{refine()}}, \code{\link[=compile_rulebook]{compile_rulebook()}}
}
