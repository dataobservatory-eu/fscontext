% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_snapshot.R
\name{read_snapshot}
\alias{read_snapshot}
\title{Read and combine observational filesystem snapshots}
\usage{
read_snapshot(snapshot_files, include_repo_metadata = FALSE)
}
\arguments{
\item{snapshot_files}{Character vector of snapshot \code{.rds} files.}

\item{include_repo_metadata}{Logical.

If \code{TRUE}, repository metadata stored in snapshot attributes are
materialized into the returned observational table.

The following repository-level variables may be added:
\itemize{
\item \code{git_remote}
\item \code{git_branch}
\item \code{git_repo_id}
}

This may increase memory usage because repository metadata are
repeated across all observations belonging to the same repository.}
}
\value{
A \code{data.frame} containing combined filesystem observations.

The returned table contains all variables created by
\code{\link[=scan_storage]{scan_storage()}} together with additional provenance and contextual
identifiers:
\itemize{
\item \code{snapshot_file}:
normalized path of the source snapshot artefact
\item \code{snapshot_created_at}:
observation timestamp recorded in snapshot metadata
\item \code{snapshot_schema_version}:
schema version recorded in snapshot metadata
\item \code{storage_full_path}:
globally contextualized filesystem locator
(\code{storage_id::full_path})
\item \code{storage_path_id}:
storage-scoped logical filesystem identifier
(\code{storage_id::rel_path})
\item \code{observation_id}:
identifier of a specific filesystem observation event,
combining storage context, logical path, and observation time
}
}
\description{
\code{read_snapshot()} reconstructs an observational layer from one or
more snapshots previously created with \code{\link[=scan_storage]{scan_storage()}}.

The function preserves filesystem observations as originally recorded,
while appending snapshot-level provenance and contextual identifiers
that support longitudinal and cross-storage analytical workflows.

The resulting table is intended to represent observed filesystem
Instantiations rather than authoritative documentary entities.
}
\details{
Read one or more serialized observational filesystem snapshots and
combine them into a unified observational table.

The function performs:
\itemize{
\item observational aggregation across snapshots
\item snapshot-level provenance preservation
\item contextual identifier enrichment
\item optional materialization of repository-level Git metadata
}

The function intentionally does not:
\itemize{
\item deduplicate observations
\item infer stable file identity
\item infer Record Resources or Record Sets
\item resolve documentary semantics
\item interpret provenance relationships
}

Multiple observations of the same filesystem approximation may occur:
\itemize{
\item across observation times
\item across storage contexts
\item across partially overlapping snapshots
\item across synchronized or copied working environments
}

In RiC-aligned operational terms:
\itemize{
\item each row represents one observed filesystem Instantiation
\item repeated observations may later support inference of more stable
Record Resources
\item higher-level documentary interpretation is deferred to later
analytical or curatorial stages
}

Snapshot-level provenance metadata are appended as columns to support:
\itemize{
\item provenance-aware analytics
\item reconstruction workflows
\item cross-storage comparison
\item longitudinal temporal analysis
}

Repository metadata are normally stored as snapshot attributes in
order to avoid repeating identical repository information across all
observations. When \code{include_repo_metadata = TRUE}, repository-level
metadata are materialized into the returned table to support
repository-aware analytical workflows.
}
