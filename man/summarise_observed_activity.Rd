% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise_observed_activity.R
\name{summarise_observed_activity}
\alias{summarise_observed_activity}
\alias{summarize_activity}
\title{Summarise observed activity from filesystem observations}
\usage{
summarise_observed_activity(
  df,
  extensions = c("r", "bak"),
  path_col = "rel_path",
  time_unit = c("week", "month", "day", "year"),
  max_files = 20
)

summarize_activity(
  df,
  extensions = c("r", "bak"),
  path_col = "rel_path",
  time_unit = c("week", "month", "day", "year"),
  max_files = 20
)
}
\arguments{
\item{df}{A \code{data.frame} representing filesystem observations.
The data must contain:
\itemize{
\item \code{rel_path};
\item \code{filename};
\item \code{mtime};
\item \code{extension}.
}

If present, \code{git_tracked} is used to count untracked files.}

\item{extensions}{Character vector of file extensions to include,
without leading dots. Matching is case-insensitive.}

\item{path_col}{Character scalar. Name of the column containing
paths used to derive structural groupings. Defaults to \code{"rel_path"}.}

\item{time_unit}{Character scalar. One of \code{"week"}, \code{"month"},
\code{"day"}, or \code{"year"}.}

\item{max_files}{Integer. Maximum number of file names displayed
in each summary row.}
}
\value{
A \code{data.frame} with one row per \code{period} and \code{group_path}
combination.

The returned columns include:

\describe{
\item{period}{
Time bucket identifier.
}
\item{group_path}{
Structural grouping key derived from \code{path_col}.
}
\item{start}{
Earliest observed modification date in the group.
}
\item{end}{
Latest observed modification date in the group.
}
\item{file_names}{
Pipe-separated sample of observed file names.
}
\item{n_files}{
Number of file observations in the group.
}
\item{n_unique_files}{
Number of distinct paths in the group.
}
\item{untracked}{
Number of observations not tracked by Git, when \code{git_tracked}
is available; otherwise \code{NA_integer_}.
}
}
}
\description{
\code{summarise_observed_activity()} summarises observed file-level
modification evidence by time period and structural grouping.

The function is intended as a temporal contextualisation step.
It does not infer archival Activities, Events, Record Sets, or
provenance relations. Instead, it produces candidate activity
summaries that may support later human review, semantic
stabilisation, or RiC-aligned modelling.
}
\details{
Aggregates filesystem observations into reproducible temporal
summaries grouped by structural path context.

The function derives:
\itemize{
\item \code{period}, a time bucket derived from file modification times
(\code{mtime});
\item \code{group_path}, a structural grouping key derived from the selected
path column.
}

It then summarises observations within each
\code{period} x \code{group_path} combination.

Modification times are treated as observational evidence of change.
They are not interpreted as complete editing histories or confirmed
archival events.

Structural grouping is deterministic and based on path structure.
It is intended for aggregation, review, and reporting, not for
file identity. Use \code{rel_path} for file-level identity.

Typical uses include:
\itemize{
\item identifying temporal clusters of filesystem activity;
\item reviewing candidate Activities before semantic stabilisation;
\item comparing activity across structural contexts;
\item supporting archival recontextualisation workflows;
\item preparing analytical or audit summaries.
}

Files under \code{.Trash} are excluded.
}
\examples{
data("fscontextdemo_snapshot_02")

summarise_observed_activity(
  fscontextdemo_snapshot_02,
  time_unit = "month"
)

}
\seealso{
\code{\link[=project_record_events]{project_record_events()}}, \code{\link[=path_prefix]{path_prefix()}}
}
