% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise_activity.R
\name{summarise_activity}
\alias{summarise_activity}
\alias{summarize_activity}
\title{Summarise file activity by time period and structural path}
\usage{
summarise_activity(
  df,
  extensions = c("r", "bak"),
  path_col = "rel_path",
  time_unit = c("week", "month", "day", "year"),
  max_files = 20
)

summarize_activity(
  df,
  extensions = c("r", "bak"),
  path_col = "rel_path",
  time_unit = c("week", "month", "day", "year"),
  max_files = 20
)
}
\arguments{
\item{df}{A \code{data.frame} representing a filesystem snapshot. Must conform
to the canonical schema (see \code{\link[=normalise_snapshot_schema]{normalise_snapshot_schema()}}), including:
\itemize{
\item \code{rel_path}
\item \code{filename}
\item \code{mtime} (POSIXct)
\item \code{extension}
\item optionally \code{git_tracked}
}}

\item{extensions}{Character vector of file extensions to include
(case-insensitive, without leading dots).}

\item{path_col}{Character. Name of the column containing file paths
(default: \code{"rel_path"}).}

\item{time_unit}{One of \code{"week"}, \code{"month"}, \code{"day"}, \code{"year"}.}

\item{max_files}{Integer. Maximum number of file names shown per group.}
}
\value{
A \code{data.frame} with one row per (period × group_path), containing:
\describe{
\item{period}{Time bucket identifier (e.g. \code{"2026-17"}).}
\item{group_path}{Project-level grouping derived from the first components
of \code{rel_path}, typically representing project and module (e.g.
\verb{_packages/iocodelists/R}).}
\item{start}{Earliest modification date in the group.}
\item{end}{Latest modification date in the group.}
\item{file_names}{Pipe-separated list of filenames (truncated).}
\item{n_files}{Number of file observations in the group.}
\item{n_unique_files}{Number of distinct files (\code{rel_path}) in the group.}
\item{untracked}{Number of files not tracked by Git (if available).}
}
}
\description{
Aggregates file-level observations (e.g. from \code{scan_storage()}) into
time-based summaries grouped by a deterministic structural path prefix.
}
\details{
The function derives:
\itemize{
\item a \emph{time bucket} (\code{period}) from file modification times (\code{mtime})
\item a \emph{grouping key} (\code{group_path}) derived from the project and its
immediate subdirectory (module), using an internal structural parser
}

and summarises activity within each (period × group_path) combination.

This provides a reproducible, structure-aware view of observed activity,
suitable for exploratory analysis, forensic reconstruction, and audit
workflows.

This function operates on \strong{observational data}:
\itemize{
\item grouping is structural and deterministic, based on the first components
of \code{rel_path}, typically corresponding to project and module folders
(e.g. \code{R}, \code{tests}, \code{data-raw})
\item no assumptions are made about project structure or file roles
\item identical inputs always produce identical outputs
}

The \code{group_path} is a \strong{project–module level projection of \code{rel_path}}.
It is derived by extracting the first components of the path (e.g.
\verb{_packages/iocodelists/R}) and is intended for aggregation and reporting.

The output is intended for \strong{analysis and reporting}, not for
file-level identity or joins. For identity, use \code{rel_path}.

Modification times (\code{mtime}) are treated as a proxy for activity.
They indicate observed changes, not a complete editing history.

Files under \code{.Trash} are excluded by default.
This approach aligns grouping with typical project layouts (e.g. R packages),
where the first directory levels correspond to project boundaries and
functional modules.
}
\examples{
\dontrun{
df <- scan_storage("D:/_eviota")

# Weekly overview
summarise_activity(df, time_unit = "week")

# Monthly overview
summarise_activity(df, time_unit = "month")
}

}
\seealso{
\code{\link[=path_prefix]{path_prefix()}}, \code{\link[=normalise_snapshot_schema]{normalise_snapshot_schema()}}
}
