% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise_duplicates.R
\name{summarise_duplicates}
\alias{summarise_duplicates}
\alias{summarize_duplicates}
\title{Summarise repeated and divergent filesystem observations}
\usage{
summarise_duplicates(df)

summarize_duplicates(df)
}
\arguments{
\item{df}{A snapshot \code{data.frame} conforming to the canonical
snapshot schema created by \code{\link[=scan_storage]{scan_storage()}} or
\code{\link[=read_snapshot]{read_snapshot()}}.

The dataset must contain:
\itemize{
\item \code{filename}
\item \code{quick_sig} (may contain \code{NA})
}}
}
\value{
A \code{data.frame} with one row per \code{filename}.

The returned variables include:

\describe{
\item{filename}{
File basename used as grouping key.
}

\item{total_copies}{
Total number of observed filesystem occurrences.
}

\item{identical_copies}{
Size of the largest identical-signature group.
}

\item{versioned_copies}{
Number of observations outside the largest identical-signature
group.
}

\item{n_versions}{
Number of distinct observed signatures.
}
}
}
\description{
Aggregates observed filesystem observations by filename and
lightweight content signatures (\code{quick_sig}).
}
\details{
The function identifies:
\itemize{
\item repeated identical observations;
\item potentially synchronised copies;
\item diverging versions of similarly named resources;
\item distributed working duplicates.
}

The function operates on observational filesystem evidence only.

It does not:
\itemize{
\item infer authoritative file identity;
\item establish Record Resource equivalence;
\item reconstruct provenance lineage;
\item determine curatorial relationships.
}

In RiC-aligned operational terminology:
\itemize{
\item rows in the snapshot represent filesystem observations;
\item repeated identical \code{quick_sig} values provide operational
evidence that multiple observations may correspond to the
same underlying digital resource;
\item differing signatures associated with the same filename may
indicate divergent versions, forks, or independently evolving
resources.
}

The function therefore supports:
\itemize{
\item longitudinal reconstruction;
\item distributed workflow analysis;
\item duplicate detection;
\item exploratory Record Set construction;
\item provenance-aware analytical workflows.
}

Duplicate observations are not inherently anomalous.

In distributed development workflows the same file may legitimately
appear:
\itemize{
\item across multiple machines;
\item across synchronised project folders;
\item in backup or staging locations;
\item in derived analytical Record Sets.
}

The function therefore reports observational duplication rather
than asserting erroneous copying.

The function treats:
\itemize{
\item \code{filename} as a weak identity signal;
\item \code{quick_sig} as a lightweight content equivalence signal.
}

Missing signatures (\code{NA}) are treated as a valid observational group.

This means:
\itemize{
\item multiple \code{NA} signatures are considered identical;
\item a mix of \code{NA} and non-\code{NA} signatures counts as versioning.
}

The function operates on observational snapshots and does not
resolve identity across time or storage contexts.
}
\examples{
data("fscontextdemo_snapshot_01")
data("fscontextdemo_snapshot_01")

combined_snapshot <- rbind(
  fscontextdemo_snapshot_01,
  fscontextdemo_snapshot_01
)

summarise_duplicates(combined_snapshot)

}
\seealso{
\code{\link[=quick_signature]{quick_signature()}}
}
