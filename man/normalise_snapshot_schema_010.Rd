% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalise_snapshot_schema.R
\name{normalise_snapshot_schema_010}
\alias{normalise_snapshot_schema_010}
\title{Normalise snapshot schema from version 0.1.0 to 0.1.2}
\usage{
normalise_snapshot_schema_010(df)
}
\arguments{
\item{df}{A \code{data.frame} representing a filesystem snapshot.}
}
\value{
A \code{data.frame} with canonical column names and derived variables.
}
\description{
Migrates a snapshot \code{data.frame} created with earlier versions of the
package (pre-0.1.2) to the canonical schema used from version 0.1.2 onward.
}
\details{
This function performs non-destructive transformations to align column names
and derived variables with the current naming contract:
\itemize{
\item derives \code{dir_path} from \code{rel_path} if missing
\item renames \code{file} to \code{filename} where applicable
\item renames \code{folder} to \code{group_path} where applicable
}

The function is designed to be:
\itemize{
\item idempotent (safe to run multiple times)
\item conservative (does not overwrite existing canonical columns)
\item explicit (warns on conflicting legacy vs canonical values)
}

After normalisation, the function attaches schema metadata as attributes:
\itemize{
\item \code{schema_version = "0.1.2"}
\item \code{normalised_from = "0.1.0"}
}

This function does not remove legacy columns (\code{file}, \code{folder}) but ensures
that canonical columns (\code{filename}, \code{group_path}) are present and consistent.

The resulting object conforms to the schema described in the package
vignette ("Reporting Data Structure").
}
\keyword{internal}
